name: Build and Release

on:
  push:
    branches:
      - main
    tags:
      - "v*"
  workflow_dispatch:

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            artifact_name: hostman-linux-x64
            cmake_opts: -DHOSTMAN_USE_TUI=ON
          - os: macos-latest
            artifact_name: hostman-macos-arm64
            cmake_opts: -DHOSTMAN_USE_TUI=ON

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install dependencies (Ubuntu)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake libcurl4-openssl-dev libsqlite3-dev libssl-dev pkg-config libcjson-dev libncurses-dev

      - name: Install dependencies (macOS)
        if: runner.os == 'macOS'
        run: |
          brew install cmake curl openssl sqlite cjson ncurses pkg-config

      - name: Configure CMake (macOS)
        if: runner.os == 'macOS'
        run: |
          mkdir -p build
          cd build
          cmake \
            -DCMAKE_PREFIX_PATH="$(brew --prefix)/opt/openssl@3;$(brew --prefix)" \
            -DCMAKE_LIBRARY_PATH="$(brew --prefix)/lib;$(brew --prefix)/opt/curl/lib;$(brew --prefix)/opt/openssl@3/lib;$(brew --prefix)/opt/cjson/lib" \
            -DCMAKE_INCLUDE_PATH="$(brew --prefix)/include;$(brew --prefix)/opt/curl/include;$(brew --prefix)/opt/openssl@3/include;$(brew --prefix)/opt/cjson/include" \
            -DCMAKE_C_FLAGS="-I$(brew --prefix)/opt/cjson/include" \
            -DCMAKE_EXE_LINKER_FLAGS="-L$(brew --prefix)/opt/cjson/lib" \
            ${{ matrix.cmake_opts }} ..

      - name: Configure CMake (Linux)
        if: runner.os == 'Linux'
        run: |
          mkdir -p build
          cd build
          cmake ${{ matrix.cmake_opts }} ..

      - name: Setup MSVC
        if: runner.os == 'Windows'
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: x64

      - name: Install vcpkg dependencies (Windows)
        if: runner.os == 'Windows'
        run: |
          choco install pkgutils -y
          git clone https://github.com/microsoft/vcpkg.git C:/vcpkg --depth 1
          C:/vcpkg/bootstrap-vcpkg.bat -disableMetrics
          C:/vcpkg/vcpkg integrate install
          C:/vcpkg/vcpkg install curl:x64-windows sqlite3:x64-windows cjson:x64-windows openssl:x64-windows

      - name: Configure CMake (Windows)
        if: runner.os == 'Windows'
        run: |
          mkdir -p build
          cd build
          cmake -G "Visual Studio 17 2022" -A x64 -DCMAKE_TOOLCHAIN_FILE=C:/vcpkg/scripts/buildsystems/vcpkg.cmake ..

      - name: Build
        working-directory: build
        run: cmake --build . --config Release

      - name: Create artifact
        run: |
          mkdir -p artifact
          if [ "${{ matrix.os }}" = "windows-latest" ]; then
            cp build/hostman.exe artifact/${{ matrix.artifact_name }}.exe
          else
            cp build/hostman artifact/${{ matrix.artifact_name }}
          fi

      - name: Upload artifact
        if: "${{ github.token != '' }}"
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: artifact/*
          retention-days: 5

  release:
    needs: build
    runs-on: ubuntu-latest
    if: github.ref_type == 'tag'

    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Get version from tag
        id: version
        run: |
          VERSION=${GITHUB_REF#refs/tags/v}
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "version=$VERSION"

      - name: Extract changelog
        id: changelog
        run: |
          VERSION=${{ steps.version.outputs.version }}
          CHANGELOG=$(sed -n "/^## $VERSION/,/^## /p" CHANGELOG.md | head -n -1 | tail -n +2)
          if [ -z "$CHANGELOG" ]; then
            CHANGELOG="No changelog entry found for version $VERSION"
          fi
          echo "changelog<<EOF" >> $GITHUB_OUTPUT
          echo "$CHANGELOG" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          name: v${{ steps.version.outputs.version }}
          body: |
            ## Changelog

            ${{ steps.changelog.outputs.changelog }}

            ## Downloads

            Download the appropriate binary for your platform.
          draft: false
          prerelease: false
          generate_release_notes: false
          files: |
            artifacts/hostman-linux-x64/hostman-linux-x64
            artifacts/hostman-macos-arm64/hostman-macos-arm64
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
